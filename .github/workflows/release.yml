name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-linux-gnueabi

      - name: Build for Kindle (ARM)
        run: |
          mkdir -p bin
          arm-linux-gnueabi-gcc -O2 -Wall -Wextra -static -o bin/kindlejap-bin src/kindlejap.c -lpthread
          arm-linux-gnueabi-strip bin/kindlejap-bin
          chmod +x bin/kindlejap-bin
          chmod +x bin/kindlejap.sh
          chmod +x bin/update.sh

      - name: Create release package
        run: |
          mkdir -p release/japlat/bin
          cp bin/kindlejap-bin release/japlat/bin/
          cp bin/kindlejap.sh release/japlat/bin/
          cp bin/update.sh release/japlat/bin/
          cp config.xml release/japlat/
          cp menu.json release/japlat/
          cp Makefile release/japlat/
          cp README.md release/japlat/
          cp LICENSE release/japlat/
          cp CHANGELOG.md release/japlat/
          cd release
          tar -czf ../kindlejap-${GITHUB_REF_NAME}.tar.gz *

      - name: Create Release
        run: |
          gh release create ${{ github.ref_name }} \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            kindlejap-${{ github.ref_name }}.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
